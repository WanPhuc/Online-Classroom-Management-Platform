@model BTL_QuanLyLopHocTrucTuyen.Models.Lesson
@{
    ViewData["Title"] = "Add New Lesson (VerifyKey)";
    Layout = "~/Views/Instructor/Shared/_InstructorLayout.cshtml";
}

<link rel="stylesheet" href="~/css/Instructor/Lesson/AddLesson.css" asp-append-version="true" />

<div class="content-main">
    <h3><i class="bi bi-plus-circle"></i> Thêm bài học mới (có VerifyKey)</h3>

    <form id="newLessonForm" method="post">
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <div class="mb-3">
            <label asp-for="Title" class="form-label">Tên bài học *</label>
            <input asp-for="Title" class="form-control" placeholder="Tên bài học ..." />
            <span asp-validation-for="Title" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="VerifyKey" class="form-label">Verify Key *</label>
            <input asp-for="VerifyKey" class="form-control" placeholder="Nhập 10 ký tự, bắt đầu bằng số" />
            <span asp-validation-for="VerifyKey" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Content" class="form-label">Mô tả</label>
            <textarea asp-for="Content" rows="5" class="form-control" placeholder="Mô tả ..."></textarea>
            <span asp-validation-for="Content" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="VideoUrl" class="form-label">Link bài học</label>
            <input asp-for="VideoUrl" class="form-control" placeholder="Link bài học ..." />
            <span asp-validation-for="VideoUrl" class="text-danger"></span>
        </div>

        <div class="row">
            <div class="col-md-6 mb-3">
                <label asp-for="BeginTime" class="form-label">Thời gian bắt đầu</label>
                <input asp-for="BeginTime" type="datetime-local" class="form-control" />
                <span asp-validation-for="BeginTime" class="text-danger"></span>
            </div>
            <div class="col-md-6 mb-3">
                <label asp-for="EndTime" class="form-label">Thời gian kết thúc</label>
                <input asp-for="EndTime" type="datetime-local" class="form-control" />
                <span asp-validation-for="EndTime" class="text-danger"></span>
            </div>
        </div>

        <div class="d-flex justify-content-between mt-4">
            <a asp-action="NewLesson" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Quay lại
            </a>
            <button type="submit" class="btn btn-primary">
                <i class="bi bi-floppy2"></i> Lưu bài học
            </button>
        </div>
    </form>

    <div id="resultMessage" class="mt-3 fw-semibold"></div>
</div>

@section Scripts{
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script>
        $(document).ready(function () {
            $("#newLessonForm").on("submit", function (e) {
                e.preventDefault();
                const formData = $(this).serialize();

                $.ajax({
                    url: '/Instructor/AddNewLesson', 
                    type: 'POST',
                    data: formData,
                    success: function (res) {
                        if (res.success) {
                            $("#resultMessage").text("✅ Thêm bài học thành công!");
                            $("#newLessonForm")[0].reset();
                        } else {
                            $("#resultMessage").text("❌ " + (res.message || res.errors?.join(", ")));
                        }
                    },
                    error: function () {
                        $("#resultMessage").text("⚠️ Có lỗi xảy ra khi gửi dữ liệu!");
                    }
                });
            });
        });
    </script>
}
